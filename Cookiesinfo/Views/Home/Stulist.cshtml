@model IEnumerable<Cookiesinfo.Models.stuimg>

@{
    ViewBag.Title = "Stulist";
}

<h2>Stulist</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.img_path }) |
            @Html.ActionLink("Details", "Details", new { id=item.img_path }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.img_path })
        </td>
    </tr>
}

</table>
